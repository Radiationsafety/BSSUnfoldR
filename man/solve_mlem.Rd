% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_mlem.R
\name{solve_mlem}
\alias{solve_mlem}
\title{MLEM (Maximum Likelihood Expectation Maximization) unfolding}
\usage{
solve_mlem(A, b, x0, max_iterations = 1000L, tolerance = 1e-06)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum guess (length n).}

\item{max_iterations}{Positive integer; default 1000.}

\item{tolerance}{Positive numeric; convergence tolerance on the relative
spectrum change. Default 1e-6.}
}
\value{
A list \code{list(spectrum = ..., iterations = ..., converged = ...)}.
}
\description{
Iteratively solves \eqn{A x = b} with the multiplicative update
\eqn{x_{k+1} = x_k \cdot (A^T (b / (A x_k)))}{x_{k+1} = x_k * (A' * (b / (A x_k)))}.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_mlem(A, b, rep(1, 3), max_iterations = 50)
r$converged
}
