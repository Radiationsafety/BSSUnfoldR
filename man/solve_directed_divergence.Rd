% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_directed_divergence.R
\name{solve_directed_divergence}
\alias{solve_directed_divergence}
\title{Directed-divergence unfolding}
\usage{
solve_directed_divergence(
  A,
  b,
  x0,
  max_iterations = 200L,
  tol_chi2 = 1,
  tol_rel = 1e-06,
  relative_uncertainty = 0.05,
  sigma = NULL,
  smoothness_order = 0L,
  smoothness_weight = 0
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum guess (length n).}

\item{max_iterations}{Positive integer; default 200.}

\item{tol_chi2}{Numeric; chi-squared stopping threshold. Default 1.0.}

\item{tol_rel}{Numeric; relative-change stopping threshold. Default 1e-6.}

\item{relative_uncertainty}{Numeric; relative measurement uncertainty used
to derive sigma when \code{sigma} is not given. Default 0.05.}

\item{sigma}{Optional explicit per-detector uncertainties.}

\item{smoothness_order}{Integer; 0, 1, or 2. Default 0.}

\item{smoothness_weight}{Numeric non-negative penalty weight. Default 0.0.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_directed_divergence.py}. Minimizes
the Poisson/I-divergence data term with multiplicative updates. Optional
first- or second-order Tikhonov smoothing is applied as a non-negative
proximal step after each update.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_directed_divergence(A, b, rep(1, 3), max_iterations = 50)
}
