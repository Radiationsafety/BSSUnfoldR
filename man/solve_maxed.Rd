% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_maxed.R
\name{solve_maxed}
\alias{solve_maxed}
\title{MAXED unfolding (Maximum Entropy Deconvolution)}
\usage{
solve_maxed(
  A,
  b,
  x0,
  sigma_factor = 0.1,
  max_iterations = 5000L,
  tolerance = 1e-06
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric reference (prior) spectrum (length n).}

\item{sigma_factor}{Numeric; relative measurement uncertainty. Default 0.1.}

\item{max_iterations}{Positive integer; default 5000.}

\item{tolerance}{Positive numeric; gradient convergence tolerance. Default 1e-6.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Implements the MAXED algorithm of Reginatto & Goldhagen (1999) by
minimising, in log-space \eqn{y_i = ln(x_i)}, the primal
\deqn{f(x) = -S(x) + 0.5 * sum_j (b_j - (A x)_j)^2 / sigma_j^2}
where \eqn{S(x) = -sum_i x_i ln(x_i / x0_i) + sum_i x_i - sum_i x0_i} is
the Shannon entropy relative to the reference spectrum \code{x0}. The
L-BFGS-B optimiser of \code{optim} is used.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_maxed(A, b, rep(1, 3))
}
