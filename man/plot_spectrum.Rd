% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_spectrum}
\alias{plot_spectrum}
\title{Plot an unfolded spectrum}
\usage{
plot_spectrum(result, logx = TRUE, logy = TRUE)
}
\arguments{
\item{result}{A result list (\code{energy}, \code{spectrum}).}

\item{logx}{Logical; log-scale energy axis. Default TRUE.}

\item{logy}{Logical; log-scale fluence axis. Default TRUE.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot an unfolded spectrum
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
det_names <- c("d1", "d2", "d3")
sens <- lapply(setNames(det_names, det_names), function(n) A[which(det_names == n), ])
E <- c(1e-9, 1e-6, 1e-3)
res <- unfold_mlem(det_names, 3L, E, sens, NULL, NULL, c(d1=1, d2=.6, d3=.4))
\donttest{if (requireNamespace("ggplot2", quietly = TRUE)) { print(plot_spectrum(res)) }}
}
