% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_lanczos.R
\name{solve_lanczos}
\alias{solve_lanczos}
\title{Lanczos-hybrid (Krylov-GCV) unfolding}
\usage{
solve_lanczos(
  A,
  b,
  x0 = NULL,
  max_iterations = NULL,
  regularization = 1e-08,
  noise_level = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Ignored; accepted for API compatibility.}

\item{max_iterations}{Positive integer; Krylov dimension. Default
\code{NULL} = \code{min(nrow(A), ncol(A))}.}

\item{regularization}{Numeric; fallback regularization used if GCV returns
a degenerate value. Default 1e-8.}

\item{noise_level}{Optional numeric; relative noise level used for
discrepancy-principle early stopping. Default \code{NULL}.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_lanczos.py}. Performs Golub-Kahan
bidiagonalization of \code{A}, building a sequence of Krylov subspaces. On
the projected problem \code{min ||B_k y - bhat||^2} a Tikhonov term
\code{lambda * ||y||^2} is added, where \code{lambda} is selected
automatically by Generalized Cross Validation (GCV) at each iteration.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_lanczos(A, b, NULL, max_iterations = 3)
}
