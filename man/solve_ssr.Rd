% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_ssr.R
\name{solve_ssr}
\alias{solve_ssr}
\title{Solve by SSR}
\usage{
solve_ssr(
  A,
  b,
  x0 = NULL,
  threshold_growth = 1.6,
  n_thresholds = 12L,
  max_iterations = 50L,
  statistic_window = 5L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{threshold_growth}{Numeric multiplicative growth of the threshold
sweep. Default 1.6.}

\item{n_thresholds}{Integer number of thresholds swept. Default 12.}

\item{max_iterations}{Integer; steps of the QSOR sign-sweep. Default 50.}

\item{statistic_window}{Integer; window of the minimum-statistics
scoring. Default 5.}
}
\value{
A list \code{list(spectrum, iterations, converged, threshold)}.
}
\description{
Solve by SSR
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_ssr(A, b, rep(1, 3), max_iterations = 30L)
}
