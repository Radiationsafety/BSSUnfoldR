% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_mapem.R
\name{solve_mapem}
\alias{solve_mapem}
\title{MAP-EM (penalised expectation maximisation) unfolding}
\usage{
solve_mapem(
  A,
  b,
  x0,
  prior = "quadratic",
  beta = 0.001,
  prior_delta = 1,
  gamma = 1,
  max_iterations = 50L,
  tolerance = 1e-06
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{prior}{Character: \code{"none"}, \code{"quadratic"},
\code{"logcosh"} or \code{"relative_difference"}. Default
\code{"quadratic"}.}

\item{beta}{Numeric prior weight. Default 1e-3.}

\item{prior_delta}{Numeric width/floor parameter. Default 1.0.}

\item{gamma}{Numeric edge-preservation parameter. Default 1.0.}

\item{max_iterations}{Positive integer; default 50.}

\item{tolerance}{Positive numeric; default 1e-6.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_mapem.py} (one-step-late OSMAPOSL).
The update reads
\deqn{x^{n+1} = x^n * A^T ( b / (A x^n + eps) ) / ( A^T 1 + beta * grad V(x^n) )}
with \eqn{V} a nearest-neighbour prior over the energy axis. Available
priors: \code{quadratic}, \code{logcosh}, \code{relative_difference}. Set
\code{prior = "none"} to recover plain MLEM.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_mapem(A, b, rep(1, 3), prior = "quadratic")
}
