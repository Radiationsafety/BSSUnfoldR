% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_combined.R
\name{solve_combined}
\alias{solve_combined}
\title{Solve by named-method pipeline}
\usage{
solve_combined(A, b, x0 = NULL, pipeline = NULL, ...)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{pipeline}{List of stages; each stage is a list with fields
\code{method} (character) and optional \code{params} (list of extra
arguments for the stage solver).}

\item{...}{Extra arguments forwarded to every stage solver.}
}
\value{
A list \code{list(spectrum, iterations, converged,
  pipeline_spectra)}.
}
\description{
Solve by named-method pipeline
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_combined(A, b, rep(1, 3),
                    pipeline = list(
                        list(method = "mlem",
                             params = list(max_iterations = 30L)),
                        list(method = "landweber",
                             params = list(max_iterations = 10L))))
}
