% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validators.R
\name{validators}
\alias{validators}
\alias{validate_readings}
\alias{validate_energy_grid}
\alias{validate_spectrum}
\alias{validate_response_matrix}
\alias{validate_system}
\title{Input validation utilities for BSSUnfoldR}
\usage{
validate_readings(readings, detector_names, allow_zero = TRUE)

validate_energy_grid(E_MeV, min_points = 2L, Emin = NULL, Emax = NULL)

validate_spectrum(spectrum, E_MeV, allow_negative = FALSE)

validate_response_matrix(A, b, check_rank = FALSE)

validate_system(A, b, x0 = NULL, max_iterations = NULL, tolerance = NULL)
}
\arguments{
\item{readings}{Named numeric vector of detector readings.}

\item{detector_names}{Character vector of allowed detector names.}

\item{allow_zero}{Logical; if \code{FALSE} zero readings are rejected.
Default \code{TRUE}.}

\item{E_MeV}{Numeric vector of energy grid points.}

\item{min_points}{Integer; minimum required length. Default 2.}

\item{Emin}{Optional numeric lower bound. Default \code{NULL} (no bound).}

\item{Emax}{Optional numeric upper bound. Default \code{NULL} (no bound).}

\item{spectrum}{Numeric vector of spectrum values.}

\item{allow_negative}{Logical; if \code{TRUE} negative values are accepted.
Default \code{FALSE}.}

\item{A}{Numeric matrix (m x n).}

\item{b}{Numeric vector of length m.}

\item{check_rank}{Logical; if \code{TRUE} warns when A is rank-deficient.
Default \code{FALSE}.}

\item{x0}{Optional initial guess (length n). Default \code{NULL}.}

\item{max_iterations}{Optional positive integer. Default \code{NULL}.}

\item{tolerance}{Optional non-negative numeric. Default \code{NULL}.}
}
\value{
A named numeric vector with the validated readings.

A numeric vector (the validated energy grid).

The validated spectrum as a numeric vector.

A list \code{list(A = A, b = b)} with validated values.

A list \code{list(A = A, b = b, x0 = x0)} with validated values.
}
\description{
Helper functions that validate detector readings, energy grids, spectra,
and the response matrix / measurement vector pair used by every solver.
These mirror the corresponding validators in the Python \code{bssunfold}
package (\code{utils/validators.py}).

\code{validate_readings} filters the readings list to the names present in
\code{detector_names} and rejects negative, infinite or NaN values.

\code{validate_energy_grid} checks that the energy grid is a strictly
increasing positive numeric vector of length at least \code{min_points}.

\code{validate_spectrum} checks that a spectrum vector is finite, the right
length, and (by default) non-negative.

\code{validate_response_matrix} checks the dimensions and finiteness of the
response matrix \code{A} and measurement vector \code{b}.

\code{validate_system} is the convenience wrapper used by every iterative
solver; it checks shapes, dimension compatibility and (optionally) the
positivity of \code{max_iterations} and \code{tolerance}.
}
\examples{
readings <- c("0in" = 100, "3in" = 200, "12in" = 50)
validate_readings(readings, c("0in", "3in", "12in"))
E <- 10^seq(-9, 1, length.out = 10)
validate_energy_grid(E)
E <- 10^seq(-9, 1, length.out = 10)
spec <- rep(0.5, 10)
validate_spectrum(spec, E)
A <- matrix(c(1.0, 0.5, 0.2, 0.4), nrow = 2)
b <- c(1, 0.6)
validate_response_matrix(A, b)
A <- matrix(c(1.0, 0.5, 0.2, 0.4), nrow = 2)
b <- c(1, 0.6)
x0 <- c(0.5, 0.5)
v <- validate_system(A, b, x0 = x0, max_iterations = 100L, tolerance = 1e-6)
}
