% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_zfit.R
\name{solve_zfit_poisson}
\alias{solve_zfit_poisson}
\title{Poisson-likelihood unfolding with smoothness prior}
\usage{
solve_zfit_poisson(
  A,
  b,
  x0 = NULL,
  smoothness_weight = 0.001,
  use_mcmc = FALSE,
  n_samples = 100L,
  regularization = 0,
  fallback_optimizer = "lbfgsb"
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m) (counts \eqn{k}).}

\item{x0}{Numeric initial spectrum (length n).}

\item{smoothness_weight}{Numeric second-difference prior weight.
Default 1e-3.}

\item{use_mcmc}{Logical; sample the (Gaussian-approximated) posterior
for error bands. Default FALSE.}

\item{n_samples}{Integer size of the posterior sample. Default 100.}

\item{regularization}{Numeric additional L2 prior weight. Default 0.}

\item{fallback_optimizer}{Character; \code{"lbfgsb"} (default) or
\code{"nelder"}.}
}
\value{
A list \code{list(spectrum, iterations, converged)} plus
\code{posterior_samples} when \code{use_mcmc = TRUE}.
}
\description{
Poisson-likelihood unfolding with smoothness prior
}
\examples{
A <- matrix(c(2.0, 0.10, 0.02, 0.20, 1.5, 0.15, 0.40, 0.40, 0.80),
            nrow = 3, byrow = TRUE)
k <- c(21.5, 10.0, 4.5)
r <- solve_zfit_poisson(A, k, rep(3, 3))
}
