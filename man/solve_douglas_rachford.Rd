% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_pdhg.R
\name{solve_douglas_rachford}
\alias{solve_douglas_rachford}
\title{Douglas-Rachford splitting for composite L2 + TV (+ non-negativity)}
\usage{
solve_douglas_rachford(
  A,
  b,
  x0 = NULL,
  use_tv = TRUE,
  tv_weight = 0.05,
  relaxation = 1,
  tolerance = 1e-06,
  max_iterations = 500L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{use_tv}{Logical; include the TV dual block. Default TRUE.}

\item{tv_weight}{Numeric weight of the TV term (default 0.05; use 0 for
a pure non-negative L2 solve).}

\item{relaxation}{Numeric relaxation \eqn{\gamma \in (0,2)}, default 1.0.}

\item{tolerance}{Numeric relative tolerance. Default 1e-6.}

\item{max_iterations}{Integer iterations. Default 500.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_douglas_rachford.py}: splits
\eqn{f = \tfrac12\|Ax-b\|^2} and \eqn{g = w\mathrm{TV} + \iota_{\{x\ge0\}}}
with the classical Douglas-Rachford iteration
\eqn{u \leftarrow (1-\gamma)u + \gamma\;\mathrm{prox}_f(
2\,\mathrm{prox}_g(u) - u)}.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_douglas_rachford(A, b, rep(1, 3), max_iterations = 200L)
}
