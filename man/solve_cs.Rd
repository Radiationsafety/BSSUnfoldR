% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_cs.R
\name{solve_cs}
\alias{solve_cs}
\title{Main CS solver}
\usage{
solve_cs(
  A,
  b,
  x0 = NULL,
  n_atoms = NULL,
  sparsity = 5L,
  dictionary = NULL,
  sigma_min = 0.01,
  sigma_decrease_factor = 0.5,
  mu_0 = 1,
  L = 3L,
  max_iterations = 1000L,
  tolerance = 1e-06,
  random_state = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Unused.}

\item{n_atoms}{Integer; number of dictionary atoms (for K-SVD). Default
\code{NULL} = use identity dictionary.}

\item{sparsity}{Integer; sparsity for OMP/SL0. Default 5.}

\item{dictionary}{Optional pre-computed dictionary (n x n_atoms). Default
\code{NULL} = identity (so \eqn{x = \alpha}).}

\item{sigma_min, sigma_decrease_factor, mu_0, L, max_iterations, tolerance}{SL0 parameters.}

\item{random_state}{Optional seed.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Main CS solver
}
\examples{
A <- matrix(runif(5 * 60), nrow = 5)
b <- as.numeric(A \%*\% rep(0.5, 60))
r <- solve_cs(A, b, NULL, sparsity = 10L)
}
