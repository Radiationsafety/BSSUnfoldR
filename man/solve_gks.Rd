% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_gks.R
\name{solve_gks}
\alias{solve_gks}
\title{Generalized Krylov Subspace (GKS) unfolding}
\usage{
solve_gks(
  A,
  b,
  x0 = NULL,
  smoothness_order = 0L,
  regularization_method = "gcv",
  max_iterations = NULL,
  regularization = 1e-08,
  noise_level = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Unused; accepted for API compatibility.}

\item{smoothness_order}{Integer; 0 (identity), 1, or 2. Default 0.}

\item{regularization_method}{Character; \code{"gcv"} (default), \code{"dp"},
\code{"lcurve"}, or \code{"manual"}.}

\item{max_iterations}{Optional integer; Krylov dimension. Default \code{NULL}
= \code{min(nrow(A), ncol(A))}.}

\item{regularization}{Numeric fallback regularization parameter. Default 1e-8.}

\item{noise_level}{Optional numeric; relative noise level for DP. Default
\code{NULL}.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_gks.py}. Builds a
Krylov subspace by Golub-Kahan bidiagonalization of \code{A} and projects
both \code{A} and the regularization operator \code{L} onto that subspace.
At each iteration a small projected Tikhonov problem is solved, with the
regularization parameter selected automatically by Generalized Cross
Validation (GCV), the Discrepancy Principle (DP) or the L-curve.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_gks(A, b, NULL, max_iterations = 3L)
}
