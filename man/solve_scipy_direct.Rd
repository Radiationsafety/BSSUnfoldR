% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_scipy_direct.R
\name{solve_scipy_direct}
\alias{solve_scipy_direct}
\title{Direct sparse/dense linear solvers (Scipy-direct equivalent)}
\usage{
solve_scipy_direct(
  A,
  b,
  x0 = NULL,
  tolerance = 1e-08,
  max_iterations = 4000L,
  method = "cg"
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Unused; accepted for API compatibility.}

\item{tolerance}{Positive numeric; solver tolerance. Default 1e-8.}

\item{max_iterations}{Positive integer; max iterations. Default 4000.}

\item{method}{Character: one of \code{"direct"}, \code{"cg"}, \code{"lsqr"},
\code{"gmres"}, \code{"minres"}. Default \code{"cg"}.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_scipy_direct_method.py}.
Solves the normal equations \eqn{A^T A x = A^T b} using one of several
iterative or direct solvers. The Python original wraps scipy.sparse.linalg
(cg, cgs, bicgstab, gmres, lgmres, minres, qmr, gcrotmk, tfqmr, lsqr, lsmr);
this R port provides the most useful subset (cg, lsqr-equivalent via qr.solve,
gmres-equivalent via pracma, minres-equivalent, and direct solve).
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_scipy_direct(A, b, NULL, method = "direct")
}
