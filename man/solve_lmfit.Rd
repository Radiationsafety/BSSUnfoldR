% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_optimization.R
\name{solve_lmfit}
\alias{solve_lmfit}
\title{Levenberg-Marquardt unfolding}
\usage{
solve_lmfit(
  A,
  b,
  x0 = NULL,
  alpha = 0.01,
  smoothness_order = 0L,
  max_iterations = 1000L,
  tolerance = 1e-06
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial guess. Default \code{NULL} = flat.}

\item{alpha}{Numeric; regularization parameter. Default 0.01.}

\item{smoothness_order}{Integer; 0 (identity), 1, or 2. Default 0.}

\item{max_iterations}{Integer; default 1000.}

\item{tolerance}{Numeric; default 1e-6.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_lmfit.py}.
Minimises \eqn{\|A x - b\|^2 + \alpha \|L x\|^2} using
\code{\link[stats]{optim}} with \code{L-BFGS-B} method and box constraints.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_lmfit(A, b, rep(1, 3), alpha = 0.01, max_iterations = 50)
}
