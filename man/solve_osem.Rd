% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_osem.R
\name{solve_osem}
\alias{solve_osem}
\title{OSEM (ordered-subset expectation maximisation) unfolding}
\usage{
solve_osem(A, b, x0, max_iterations = 50L, n_subsets = 1L, tolerance = 1e-06)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{max_iterations}{Positive integer; default 50.}

\item{n_subsets}{Positive integer; number of detector subsets. Default 1.}

\item{tolerance}{Positive numeric; default 1e-6.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_osem.py}. OSEM generalises MLEM by
updating the spectrum with one subset of detectors at a time, accelerating
convergence:
\deqn{x^{n+1} = x^n * A_m^T ( b_m / (A_m x^n + eps) ) / ( A_m^T 1 + eps)}
With \code{n_subsets=1} the update reduces to standard MLEM.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_osem(A, b, rep(1, 3), n_subsets = 3)
}
