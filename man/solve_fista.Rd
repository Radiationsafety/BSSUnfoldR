% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_fista.R
\name{solve_fista}
\alias{solve_fista}
\title{FISTA unfolding (Fast Iterative Shrinkage-Thresholding Algorithm)}
\usage{
solve_fista(
  A,
  b,
  x0 = NULL,
  max_iterations = 500L,
  tolerance = 1e-08,
  regularization = 0,
  l1_penalty = 0,
  tv_penalty = 0,
  nonnegativity = TRUE,
  x_min = 0,
  x_max = Inf,
  noise_level = NULL,
  eta = 1.01
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial spectrum (length n). Default \code{NULL} = scaled ones.}

\item{max_iterations}{Positive integer; default 500.}

\item{tolerance}{Positive numeric; default 1e-8.}

\item{regularization}{Numeric Tikhonov regularization parameter. Default 0.}

\item{l1_penalty}{Numeric L1 penalty for sparsity. Default 0.}

\item{tv_penalty}{Numeric total-variation penalty. Default 0.}

\item{nonnegativity}{Logical; enforce non-negativity. Default \code{TRUE}.}

\item{x_min, x_max}{Numeric box bounds. Default \code{0} and \code{Inf}.}

\item{noise_level}{Optional numeric; relative noise level for discrepancy
principle stopping. Default \code{NULL}.}

\item{eta}{Numeric; safety factor for discrepancy principle. Default 1.01.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_fista.py}.
Accelerated proximal gradient method achieving O(1/k^2) convergence.
Supports L1, TV, Tikhonov regularization, and box constraints.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_fista(A, b, rep(1, 3), max_iterations = 50,
                 regularization = 0.01)
}
