% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_mlem_odl.R
\name{solve_mlem_odl}
\alias{solve_mlem_odl}
\title{MLEM via the operator interface (ODL analogue)}
\usage{
solve_mlem_odl(
  A,
  b,
  x0 = NULL,
  n_bins = NULL,
  max_iterations = 200L,
  tolerance = 1e-06,
  nonneg = TRUE
)
}
\arguments{
\item{A}{Response matrix (m x n) or a list \code{list(op, opadjoint)}
with callables; or callable forward operator A(x).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{n_bins}{Integer number of spectral bins (only needed when \code{A}
is callable and \code{x0} is missing).}

\item{max_iterations}{Integer MLEM iterations. Default 200.}

\item{tolerance}{Numeric relative tolerance. Default 1e-6.}

\item{nonneg}{Logical clip negative bins every iteration. Default TRUE.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
MLEM via the operator interface (ODL analogue)
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_mlem_odl(A, b, rep(1, 3), max_iterations = 50L)
}
