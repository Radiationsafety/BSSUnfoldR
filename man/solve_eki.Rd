% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_eki.R
\name{solve_eki}
\alias{solve_eki}
\title{Ensemble Kalman Inversion (EKI) unfolding}
\usage{
solve_eki(
  A,
  b,
  x0,
  n_ensemble = 50L,
  n_iterations = 50L,
  regularization = 1e-04,
  inflation = 1.02,
  noise_std = NULL,
  random_state = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial guess (length n), used as the centre of the
initial ensemble.}

\item{n_ensemble}{Integer; number of ensemble members. Default 50.}

\item{n_iterations}{Integer; number of EKI iterations. Default 50.}

\item{regularization}{Numeric; Tikhonov-style regularization added to the
covariance diagonal for numerical stability. Default 1e-4.}

\item{inflation}{Numeric; covariance inflation factor to prevent ensemble
collapse. Default 1.02.}

\item{noise_std}{Optional numeric; standard deviation of measurement noise.
If \code{NULL}, estimated as 5\% of \code{||b|| / sqrt(m)}.}

\item{random_state}{Optional integer seed.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_eki.py}.
Implements the EKI method of Iglesias et al. (2013) for Bayesian posterior
approximation without MCMC. The ensemble of particles is propagated through
the forward model and updated via the Kalman gain equation with optional
regularization for stability.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
set.seed(7)
r <- solve_eki(A, b, rep(1, 3), n_ensemble = 20, n_iterations = 30)
}
