% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_ensemble.R
\name{solve_cascade}
\alias{solve_cascade}
\title{Solve by cascade (sequential solvers, each starting from previous result)}
\usage{
solve_cascade(A, b, x0, solvers, solver_kwargs_list = NULL, ...)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{solvers}{List of solver functions, each with signature
\code{solver(A, b, x0, ...)}.}

\item{solver_kwargs_list}{Optional list of per-solver kwargs. Each entry
is a list forwarded to the corresponding solver.}

\item{...}{Extra arguments forwarded to each solver.}
}
\value{
A list \code{list(spectrum, iterations = length(solvers),
  converged = TRUE)}.
}
\description{
Solve by cascade (sequential solvers, each starting from previous result)
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
solvers <- list(solve_mlem, solve_landweber)
r <- solve_cascade(A, b, rep(1, 3), solvers,
                    max_iterations = 50L)
}
