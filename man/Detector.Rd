% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Detector.R
\name{Detector}
\alias{Detector}
\title{Detector R6 class for Bonner-sphere neutron spectrum unfolding}
\arguments{
\item{E_MeV}{Optional numeric energy grid. Default \code{NULL} = take the
energy grid of the chosen response function.}

\item{response_function}{Either a character name (one of \code{"GSF"},
\code{"PTB"}, \code{"LANL"}, \code{"JINR"}, \code{"FERMILAB"},
\code{"EURADOS"}, \code{"IHEP"}) or a named list with an \code{"E_MeV"}
entry plus one numeric vector per detector.}

\item{cc_icrp116}{Optional named list of ICRP-116 conversion coefficients.
Default \code{NULL} = use the built-in dataset interpolated to
\code{E_MeV}.}

\item{name}{Character detector name.}

\item{sensitivity}{Numeric sensitivity vector (length
\code{n_energy_bins}).}

\item{detector_names}{Character vector of detector names; must be a
subset of \code{names(self$sensitivities)}.}

\item{output}{A list (as produced by \code{\link{run_unfolding}}).}

\item{readings}{Named numeric vector.}

\item{...}{Extra arguments forwarded to \code{\link{unfold_mlem}}.}

\item{index}{Integer index into \code{self\$history} (1 = oldest).
Default \code{NULL} = the most recent entry.}

\item{cc_type}{One of \code{"ICRP116"},
\code{"ICRP74_effective"}, \code{"ICRP74_operational"},
\code{"NRB99_2009_effective"} or a named list of coefficients
with an \code{E_MeV} entry.}

\item{reference}{Either a result list with a \code{spectrum}
entry, or an explicit numeric spectrum of length
\code{n_energy_bins}.}

\item{result}{Optional result list; defaults to the latest in
\code{self\$history}.}
}
\value{
An R6 \code{Detector} object.

A list with relative-integral difference, chi-squared,
ratio statistics, and dose-rate relative difference if
available.
}
\description{
Create a new Detector object. See \code{\link{Detector}} for the
parameter documentation.

Add or overwrite a named sensitivity entry.

Set the ordered list of detector names.

Append a result list to the history.

Unfold using MLEM. See \code{\link{unfold_mlem}}.

Unfold using GRAVEL. See \code{\link{unfold_gravel}}.

Unfold using MAXED. See \code{\link{unfold_maxed}}.

Unfold using SAND-II. See \code{\link{unfold_sandii}}.

Unfold using BUNKI. See \code{\link{unfold_bunki}}.

Unfold using FERDOR. See \code{\link{unfold_ferdor}}.

Unfold using STAY'SL. See \code{\link{unfold_staysl}}.

Unfold using Landweber. See \code{\link{unfold_landweber}}.

Unfold using CGLS. See \code{\link{unfold_cgls}}.

Unfold using TSVD. See \code{\link{unfold_tsvd}}.

Unfold using OSEM. See \code{\link{unfold_osem}}.

Unfold using MAP-EM. See \code{\link{unfold_mapem}}.

Unfold using BSREM. See \code{\link{unfold_bsrem}}.

Unfold using SART. See \code{\link{unfold_sart}}.

Unfold using Kaczmarz. See \code{\link{unfold_kaczmarz}}.

Unfold using Randomized Kaczmarz. See \code{\link{unfold_randomized_kaczmarz}}.

Unfold using Lanczos. See \code{\link{unfold_lanczos}}.

Unfold using Tikhonov-Legendre. See \code{\link{unfold_tikhonov_legendre}}.

Unfold using ReBUNKI. See \code{\link{unfold_rebunki}}.

Unfold using Doroshenko. See \code{\link{unfold_doroshenko}}.

Unfold using Bayes (D'Agostini). See \code{\link{unfold_bayes}}.

Unfold using Directed divergence. See \code{\link{unfold_directed_divergence}}.

Unfold using Express. See \code{\link{unfold_express}}.

Unfold using Iterative refinement. See \code{\link{unfold_iterative_refinement}}.

Unfold using BUNKI-UT. See \code{\link{unfold_bunkiut}}.

Unfold using MLEM-STOP. See \code{\link{unfold_mlem_stop}}.

Unfold using StatReg. See \code{\link{unfold_statreg}}.

Unfold using Tikhonov-TV. See \code{\link{unfold_tikhonov_tv}}.

Unfold using GKS. See \code{\link{unfold_gks}}.

Unfold using Crystal Ball. See \code{\link{unfold_crystal_ball}}.

Unfold using IMAXED. See \code{\link{unfold_imaxed}}.

Unfold using AMAXED. See \code{\link{unfold_amaxed}}.

Unfold using FISTA. See \code{\link{unfold_fista}}.

Unfold using Bayes-spline. See \code{\link{unfold_bayes_spline_regularization}}.

Unfold using NSDUAZ. See \code{\link{unfold_nsduaz}}.

Unfold using MLEM-BS. See \code{\link{unfold_mlem_bs}}.

Unfold using NSpline. See \code{\link{unfold_nspline}}.

Unfold using MCMC. See \code{\link{unfold_mcmc}}.

Unfold using Reconst. See \code{\link{unfold_reconst}}.

Unfold using Ensemble. See \code{\link{unfold_ensemble}}.

Unfold using Cascade. See \code{\link{unfold_cascade}}.

Unfold using Composite. See \code{\link{unfold_composite}}.

Unfold using Scipy direct method. See \code{\link{unfold_scipy_direct_method}}.

Unfold using EKI. See \code{\link{unfold_eki}}.

Unfold using RFSP-JUL. See \code{\link{unfold_rfsp_jul}}.

Unfold using FRUIT-like. See \code{\link{unfold_fruit_like}}.

Unfold using AMAXED-Reg. See \code{\link{unfold_amaxed_regularization}}.

Unfold using CS. See \code{\link{unfold_cs}}.

Unfold using Bayesian parametric. See \code{\link{unfold_bayesian_parametric}}.

Unfold using Hybrid parametric. See \code{\link{unfold_hybrid_parametric}}.

Unfold using Hybrid GMRES. See \code{\link{unfold_hybrid_gmres}}.

Unfold using Binned. See \code{\link{unfold_binned}}.

Unfold using Parametric (FRUIT). See \code{\link{unfold_parametric}}.

Unfold using Parametric2 (BON95). See \code{\link{unfold_parametric2}}.

Unfold using EPIC. See \code{\link{unfold_epic}}.

Unfold using NN-KSVD. See \code{\link{unfold_nnksvd}}.

Unfold using Genetic. See \code{\link{unfold_genetic}}.

Unfold using Mystic. See \code{\link{unfold_mystic}}.

Unfold using QUBO. See \code{\link{unfold_qubo}}.

Unfold using LMfit. See \code{\link{unfold_lmfit}}.

Unfold using QPsolvers. See \code{\link{unfold_qpsolvers}}.

Unfold using CVXPY. See \code{\link{unfold_cvxpy}}.

Unfold by a named-method pipeline. See \code{\link{unfold_combined}}.

Unfold using P-spline REML. See \code{\link{unfold_pspline_reml}}.

Unfold with AMG/preconditioned Krylov. See \code{\link{unfold_amg}}.

Unfold using SSR (sign-simplicity regression). See \code{\link{unfold_ssr}}.

Unfold using the Mystic hybrid (DE + L-BFGS-B). See \code{\link{unfold_mystic_hybrid}}.

Unfold with the SMT-style exact solver. See \code{\link{unfold_smt}}.

Unfold with the SCIP-style QP engine. See \code{\link{unfold_scip}}.

Unfold with the CPLEX-style QP engine. See \code{\link{unfold_docplex}}.

Unfold with Poisson-likelihood inference. See \code{\link{unfold_zfit}}.

Unfold with MAEO (multi-algorithm evolution). See \code{\link{unfold_maeo}}.

Unfold with operator-based MLEM. See \code{\link{unfold_mlem_odl}}.

Unfold with PDHG (L2 + TV). See \code{\link{unfold_pdhg}}.

Unfold with Douglas-Rachford splitting. See \code{\link{unfold_douglas_rachford}}.

Unfold with Generalized Estimating Equations. See \code{\link{unfold_gee}}.

Unfold with an Uno-style NLP preset. See \code{\link{unfold_uno}}.

Unfold and attach an interpretation report. See \code{\link{unfold_interpret}}.

Unfold with the cvxpy-SQP parametric variant. See \code{\link{unfold_parametric_cvxpy}}.

Unfold with the qpsolvers-SQP parametric variant. See \code{\link{unfold_parametric_qpsolvers}}.

Unfold with the combined parametric variant. See \code{\link{unfold_parametric_combined}}.

Retrieve a saved result by history index.

List descriptions of all saved results
(method and residual norm per entry).

Clear the saved-result history.

Switch the dose conversion coefficient dataset
after construction (Python \code{Detector.set_dose_coefficients}).

Compare two unfolding results (or a result and a
reference spectrum) with standard metrics.

Plot the response (sensitivity) functions;
Python \code{Detector.plot_response_functions}.
\code{requireNamespace("ggplot2")} is required.

Plot the most recent unfolded spectrum with
uncertainty bands (Monte-Carlo when available); Python
\code{Detector.plot_with_uncertainty}.
}
\details{
The \code{Detector} class bundles together everything a user needs to unfold
a neutron spectrum from Bonner-sphere measurements:

\itemize{
\item an energy grid (MeV);
\item a sensitivity (response function) for each detector sphere;
\item ICRP-116 conversion coefficients for dose-rate calculation;
\item a result-history list (for retrospective comparison);
\item wrappers around all the classic unfolding algorithms provided by
this package (\code{\link{unfold_mlem}}, \code{\link{unfold_gravel}},
\code{\link{unfold_maxed}}, \code{\link{unfold_sandii}},
\code{\link{unfold_bunki}}, \code{\link{unfold_ferdor}},
\code{\link{unfold_staysl}}, \code{\link{unfold_landweber}},
\code{\link{unfold_cgls}}, \code{\link{unfold_tsvd}}).
}

Construction options cover the common cases:

\describe{
\item{\code{response_function = "PTB"}}{use the built-in PTB response
function (\code{\link{RF_PTB}}); other options: \code{"GSF"},
\code{"LANL"}, \code{"JINR"}, \code{"FERMILAB"}, \code{"EURADOS"},
\code{"IHEP"}.}
\item{\code{E_MeV = NULL}}{use the energy grid shipped with the chosen
response function.}
\item{\code{cc_icrp116 = NULL}}{use the built-in
\code{\link{ICRP116_COEFF_EFFECTIVE_DOSE}} dataset interpolated to
\code{E_MeV}.}
}
}
\section{Fields}{

\describe{
\item{\code{E_MeV}}{Numeric energy grid (MeV).}

\item{\code{n_energy_bins}}{Integer; number of energy bins.}

\item{\code{sensitivities}}{Named list of numeric sensitivity vectors.}

\item{\code{detector_names}}{Character vector of detector names to use.}

\item{\code{cc_icrp116}}{Named list of ICRP-116 conversion coefficients.}

\item{\code{history}}{List of saved unfolding results.}

\item{\code{n_detectors}}{Integer; number of entry response functions
currently registered.}
}}

\section{Methods}{

\describe{
\item{\code{add_response_function(name, sensitivities)}}{add a new named
response function to the detector.}
\item{\code{set_detector_names(...)}}{set or update the ordered list of
detector names that should be used when calling the unfold methods.}
\item{\code{unfold_mlem(readings, ...)}}{call \code{\link{unfold_mlem}}
with the detector's settings. The same pattern repeats for every
other unfold method (see the Examples section).}
\item{\code{save_result(output)}}{append a result to the history list.}
\item{\code{history}}{accessor returning the list of saved results.}
}
}

\examples{
det <- Detector$new(response_function = "PTB",
                    detector_names = c("0in", "3in", "5in", "8in", "12in"))
cat("Energy bins:", det$n_energy_bins, "\n")
cat("Detectors:", paste(det$detector_names, collapse = ", "), "\n")
}
