% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_staysl.R
\name{solve_staysl}
\alias{solve_staysl}
\title{STAY'SL unfolding (linear Bayesian update)}
\usage{
solve_staysl(
  A,
  b,
  x0,
  relative_uncertainty = 0.1,
  prior_uncertainty = 1,
  Cb = NULL,
  Cx = NULL,
  regularization = 1e-12
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric prior spectrum (length n).}

\item{relative_uncertainty}{Numeric; default 0.1.}

\item{prior_uncertainty}{Numeric; default 1.0.}

\item{Cb}{Optional explicit measurement covariance (m x m). Default \code{NULL}.}

\item{Cx}{Optional explicit prior covariance (n x n). Default \code{NULL}.}

\item{regularization}{Numeric Tikhonov term. Default 1e-12.}
}
\value{
A list \code{list(spectrum = ..., iterations = 1L, converged = TRUE)}.
}
\description{
A single-step (one-shot) linear Bayesian update:
\deqn{x = x0 + Cx A^T (Cb + A Cx A^T)^{-1} (b - A x0)}
Default covariances are diagonal: \eqn{Cb = diag((rel * b)^2)},
\eqn{Cx = diag((prior * x0)^2)}. A small Tikhonov term is added to the
bracket for numerical stability.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_staysl(A, b, rep(1, 3))
}
