% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_mystic_hybrid.R
\name{solve_mystic_hybrid}
\alias{solve_mystic_hybrid}
\title{Solve by mystic hybrid (differential-evolution global + L-BFGS-B local)}
\usage{
solve_mystic_hybrid(
  A,
  b,
  x0 = NULL,
  global_maxiter = 50L,
  global_maxfun = 2000L,
  local_maxiter = 200L,
  npop = NULL,
  regularization = 1e-04,
  smoothness = 1e-04,
  random_state = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (warm start).}

\item{global_maxiter}{Integer DE generations. Default 50.}

\item{global_maxfun}{Integer; upper bound on global stage evaluations
(Python \code{global_maxfun}). Default 2000.}

\item{local_maxiter}{Integer L-BFGS-B iterations. Default 200.}

\item{npop}{Integer population size. Default \code{max(10F, 4n)}.}

\item{regularization}{Numeric Tikhonov weight. Default 1e-4.}

\item{smoothness}{Numeric second-difference weight. Default 1e-4.}

\item{random_state}{Optional seed.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Solve by mystic hybrid (differential-evolution global + L-BFGS-B local)
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
set.seed(3)
r <- solve_mystic_hybrid(A, b, rep(1, 3), global_maxiter = 10L)
}
