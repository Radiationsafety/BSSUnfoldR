% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_tikhonov_tv.R
\name{solve_tikhonov_tv}
\alias{solve_tikhonov_tv}
\title{Tikhonov-TV (Total Variation) unfolding}
\usage{
solve_tikhonov_tv(
  A,
  b,
  x0 = NULL,
  epsilon = NULL,
  mu = c(1, 1, 1),
  max_iterations = 100L,
  type_ = "TT",
  beta = 1,
  zthr = 2.5,
  tolerance = 1e-04
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Unused; accepted for API compatibility.}

\item{epsilon}{Optional numeric; squared 2-norm of the noise. If \code{NULL},
derived from \code{noise_level} or from unregularized least-squares
residuals.}

\item{mu}{Numeric vector of length 3; penalty parameters. Default
\code{c(1.0, 1.0, 1.0)}.}

\item{max_iterations}{Positive integer; ADMM iteration count. Default 100.}

\item{type_}{Character; \code{"TT"}, \code{"TV"}, or \code{"T"}. Default \code{"TT"}.}

\item{beta}{Numeric balancing parameter between TV and Tikhonov terms.
Default 1.0.}

\item{zthr}{Numeric z-score threshold for adaptive beta (not used when
beta is numeric). Default 2.5.}

\item{tolerance}{Numeric; stabilization stopping tolerance. Default 1e-4.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_tikhonov_tv.py}.
Noise-constrained ADMM solver for:
\deqn{\min f(m) \text{ subject to } \|A m - b\|^2 = \epsilon}
where the regularizer \eqn{f} is one of:
\describe{
\item{TT}{\eqn{\|D_1 m\|_1 + \beta/2 \|D_1^{bar} g_2\|_2^2} (TV + Tikhonov, default)}
\item{TV}{\eqn{\|D_1 m\|_1} (pure total variation)}
\item{T}{\eqn{\beta/2 \|D_1^{bar} g_2\|_2^2} (pure Tikhonov)}
}
Here \eqn{D_1} is the first-derivative (TV) operator and \eqn{D_1^{bar}}
the second-derivative operator. The noise constraint is enforced through
an augmented Lagrangian / ADMM scheme.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_tikhonov_tv(A, b, NULL, max_iterations = 50)
}
