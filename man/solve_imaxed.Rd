% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_imaxed.R
\name{solve_imaxed}
\alias{solve_imaxed}
\title{IMAXED (Improved MAXED) unfolding}
\usage{
solve_imaxed(
  A,
  b,
  x0,
  sigma_factor = 0.1,
  max_iterations = 5000L,
  tolerance = 1e-08,
  line_search_tol = 1e-06
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric reference (prior) spectrum (length n).}

\item{sigma_factor}{Numeric; relative measurement uncertainty. Default 0.1.}

\item{max_iterations}{Positive integer; default 5000.}

\item{tolerance}{Positive numeric; gradient convergence tolerance. Default 1e-8.}

\item{line_search_tol}{Numeric; Armijo c1 constant. Default 1e-6.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_imaxed.py}.
Implements Improved Maximum Entropy Deconvolution (Wong 2024) using
Newton's method with Armijo backtracking line search instead of L-BFGS-B.
}
\details{
Minimises
\deqn{f(\phi) = 0.5 (A \phi - b)^T S_b (A \phi - b) + \sum_i \phi_i \ln(\phi_i / \phi_{0,i}) - \phi_i + \phi_{0,i}}
where \eqn{S_b = diag(1/\sigma^2)} and \eqn{\sigma = \sigma_factor * b}.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_imaxed(A, b, rep(1, 3), max_iterations = 50)
}
