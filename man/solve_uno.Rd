% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_uno.R
\name{solve_uno}
\alias{solve_uno}
\title{Solve the unfolding NLP with an Uno preset}
\usage{
solve_uno(
  A,
  b,
  x0 = NULL,
  preset = "filter_sqp",
  weights = "uniform",
  regularization = 0.001,
  hessian = "exact",
  max_iterations = 300L,
  tolerance = 1e-10
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial spectrum.}

\item{preset}{Character: \code{"filter_sqp"} (default) or
\code{"ipopt_like"}.}

\item{weights}{Character (\code{"uniform"} default, \code{"poisson"})
or explicit positive weight array.}

\item{regularization}{Numeric; relative roughness ridge. Default 1e-3.}

\item{hessian}{Character: \code{"exact"} (default) or \code{"bfgs"}
(interior-point preset only).}

\item{max_iterations}{Integer; max iterations. Default 300.}

\item{tolerance}{Numeric; KKT tolerance. Default 1e-10.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Solve the unfolding NLP with an Uno preset
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_uno(A, b, preset = "filter_sqp")
}
