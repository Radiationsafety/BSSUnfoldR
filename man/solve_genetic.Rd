% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_meta_heuristic.R
\name{solve_genetic}
\alias{solve_genetic}
\title{Genetic algorithm unfolding (simulated annealing)}
\usage{
solve_genetic(
  A,
  b,
  x0 = NULL,
  max_iterations = 500L,
  tolerance = 1e-06,
  regularization = 1e-04,
  smoothness = 1e-04,
  random_state = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial spectrum (warm-start). Default \code{NULL} = flat.}

\item{max_iterations}{Integer; max SA iterations. Default 500.}

\item{tolerance}{Numeric; convergence tolerance. Default 1e-6.}

\item{regularization}{Numeric; Tikhonov regularization. Default 1e-4.}

\item{smoothness}{Numeric; second-difference smoothness weight. Default 1e-4.}

\item{random_state}{Optional seed.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Searches in log space with a Landweber warm-start. The objective is
a scale-consistent residual + regularization + smoothness.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
set.seed(7)
r <- solve_genetic(A, b, rep(1, 3), max_iterations = 100)
}
