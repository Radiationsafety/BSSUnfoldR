% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_smt.R
\name{solve_smt}
\alias{solve_smt}
\title{Solve exactly (KKT/L2, min-fluence or weighted L1 fallback)}
\usage{
solve_smt(
  A,
  b,
  x0 = NULL,
  nonneg = FALSE,
  objective = "l2",
  regularization = 0
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Unused (kept for signature parity).}

\item{nonneg}{Logical; enforce non-negativity via NNLS. Default FALSE.}

\item{objective}{Character; \code{"l2"} (default), \code{"l1"},
\code{"min_fluence"}, or \code{"least_squares"}.}

\item{regularization}{Numeric; Tikhonov damping of the exact solve.
Default 0.}
}
\value{
A list \code{list(spectrum, iterations = 1L, converged = TRUE,
  objective)}.
}
\description{
Solve exactly (KKT/L2, min-fluence or weighted L1 fallback)
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_smt(A, b, NULL)
}
