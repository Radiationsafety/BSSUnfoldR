% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_pdhg.R
\name{solve_pdhg}
\alias{solve_pdhg}
\title{Solve by PDHG (L2 + TV)}
\usage{
solve_pdhg(
  A,
  b,
  x0 = NULL,
  tau = NULL,
  sigma = NULL,
  use_tv = TRUE,
  tv_weight = 0.05,
  nonnegativity = TRUE,
  tolerance = 1e-06,
  max_iterations = 500L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{tau}{Numeric; primal step. Default \code{NULL} = auto.}

\item{sigma}{Numeric; dual step. Default \code{NULL} = auto (tau chosen
so that \code{tau*sigma*||K||^2 < 1}).}

\item{use_tv}{Logical; include the TV dual block. Default TRUE.}

\item{tv_weight}{Numeric TV weight. Default 0.05.}

\item{nonnegativity}{Logical; clamp negative bins each primal update.
Default TRUE.}

\item{tolerance}{Numeric relative tolerance. Default 1e-6.}

\item{max_iterations}{Integer iterations. Default 500.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Solve by PDHG (L2 + TV)
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_pdhg(A, b, rep(1, 3), max_iterations = 100L)
}
