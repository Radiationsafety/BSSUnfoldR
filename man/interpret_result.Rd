% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_interpret.R
\name{interpret_result}
\alias{interpret_result}
\title{Interpret a previous unfold result}
\usage{
interpret_result(
  result,
  A,
  b,
  detector_names = NULL,
  regularization = 1e-04,
  n_scenarios = 20L,
  noise_level = 0.05,
  max_iterations = 200L
)
}
\arguments{
\item{result}{A result list as produced by \code{unfold_*} / Detector
methods (requires \code{spectrum}; \code{energy} is echoed when
present).}

\item{A}{Response matrix used for the unfold (m x n).}

\item{b}{Measurement vector (length m).}

\item{detector_names}{Optional detector names for row labelling.}

\item{regularization}{Numeric base regularization for the sweep labels.
Default 1e-4.}

\item{n_scenarios}{Integer number of robustness/scenario samples.
Default 20.}

\item{noise_level}{Numeric relative perturbation. Default 0.05.}

\item{max_iterations}{Integer iterations of the internal refits.
Default 200.}
}
\value{
A list with \code{robustness}, \code{shadow_prices},
\code{sensitivity_matrix}, \code{regularization_sweep}
(data frame), \code{scenarios}, \code{regularization_values}.
}
\description{
Interpret a previous unfold result
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
res <- solve_mlem(A, b, rep(1, 3), max_iterations = 50L)
report <- interpret_result(list(spectrum = res$spectrum), A, b)
names(report)
}
