% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_amg.R
\name{solve_amg}
\alias{solve_amg}
\title{Solve by AMG / preconditioned Krylov method}
\usage{
solve_amg(
  A,
  b,
  x0 = NULL,
  method = "cg",
  preconditioner = "jacobi",
  omega = 1.2,
  max_iterations = 500L,
  tolerance = 1e-06,
  outer_iterations = 10L,
  nonnegativity = TRUE,
  regularization = 0,
  smooth_sweeps = 3L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Not used (Krylov starts from zero), kept for API compatibility.}

\item{method}{Character; \code{"cg"}, \code{"bicgstab"}, or
\code{"gmres"}.}

\item{preconditioner}{Character; \code{"amg"}, \code{"jacobi"},
\code{"gs"}, \code{"sor"}, \code{"ssor"}, or \code{"none"}.}

\item{omega}{Numeric; SOR relaxation factor (for sor/ssor/amg). Default
1.2.}

\item{max_iterations}{Integer; max outer iterations. Default 500.}

\item{tolerance}{Numeric; relative residual tolerance. Default 1e-6.}

\item{outer_iterations}{Integer; number of GMRES restart cycles. Default
10.}

\item{nonnegativity}{Logical; clamp negative entries to zero at the end.
Default TRUE.}

\item{regularization}{Numeric; Tikhonov regularization added to the
normal matrix. Default 0.}

\item{smooth_sweeps}{Integer; smoothing sweeps per AMG application.
Default 3.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Solve by AMG / preconditioned Krylov method
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_amg(A, b, NULL)
}
