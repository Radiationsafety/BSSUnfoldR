% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_iterative_refinement.R
\name{solve_iterative_refinement}
\alias{solve_iterative_refinement}
\title{Iterative refinement unfolding (two-pass)}
\usage{
solve_iterative_refinement(
  A,
  b,
  x0 = NULL,
  first_pass_solver = NULL,
  second_pass_solver = NULL,
  first_pass_kwargs = NULL,
  second_pass_kwargs = NULL,
  alpha = NULL,
  max_alpha_search = 20L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial guess (length n). Default \code{NULL} = flat 0.5.}

\item{first_pass_solver}{Optional function \code{(A, b, x0, ...)} for the
first pass. Default \code{NULL} = \code{\link{solve_mlem}}.}

\item{second_pass_solver}{Optional function for the residual-correction
second pass. Default \code{NULL} = \code{\link{solve_landweber}}.}

\item{first_pass_kwargs}{Optional list of kwargs for \code{first_pass_solver}.}

\item{second_pass_kwargs}{Optional list of kwargs for
\code{second_pass_solver}.}

\item{alpha}{Optional numeric blending factor in
\code{x_final = x1 + alpha * x2}. Default \code{NULL} = auto-line search.}

\item{max_alpha_search}{Integer; line-search grid size. Default 20.}
}
\value{
A list \code{list(spectrum = ..., info = list(...))}.
}
\description{
R port of \code{bssunfold/core/unfold_iterative_refinement.py}.
Two-pass method combining a fast first-pass solver (e.g. MLEM) with a
residual-correction second-pass solver (e.g. Landweber or CGLS).
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_iterative_refinement(A, b, rep(1, 3))
}
