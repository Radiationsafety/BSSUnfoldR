% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_mlem_stop.R
\name{solve_mlem_stop}
\alias{solve_mlem_stop}
\title{MLEM with J-factor early stopping (MLEM-STOP)}
\usage{
solve_mlem_stop(
  A,
  b,
  x0,
  max_iterations = 15000L,
  cps_crossover = 30000,
  j_threshold = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial guess (length n).}

\item{max_iterations}{Positive integer; default 15000.}

\item{cps_crossover}{Numeric; crossover CPS value for automatic J threshold.
Default 30000.}

\item{j_threshold}{Optional numeric; if \code{NULL} computed as
\code{mean(b) / cps_crossover}.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_mlem_stop.py}. Based
on:
Montgomery et al., "A novel MLEM stopping criterion for unfolding neutron
fluence spectra in radiation therapy", Nucl. Instrum. Meth. A 957 (2020)
163400. \url{https://doi.org/10.1016/j.nima.2020.163400}
}
\details{
The J-factor is \eqn{J = \sum (b_i - (A x)_i)^2 / \sum (A x)_i}. Iterations
stop as soon as \eqn{J \le J_{threshold}}.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_mlem_stop(A, b, rep(1, 3), max_iterations = 100)
}
