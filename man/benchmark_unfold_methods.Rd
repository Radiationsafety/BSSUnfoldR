% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_benchmark.R
\name{benchmark_unfold_methods}
\alias{benchmark_unfold_methods}
\title{Benchmark selected unfolding methods on synthetic probes}
\usage{
benchmark_unfold_methods(
  detector,
  methods = list(landweber = list(max_iterations = 30L)),
  n_probes = 4L,
  seed = 1234L
)
}
\arguments{
\item{detector}{Detector R6 object (uses \code{sensitivities},
\code{detector_names}, \code{E_MeV}).}

\item{methods}{Named list where each entry is the parameter list for
the solver \code{method} given as the list name.}

\item{n_probes}{Integer number of synthetic spectra. Default 4.}

\item{seed}{Integer RNG seed. Default 1234.}
}
\value{
A list \code{list(results)} with per-method entries
\code{method}, \code{mean_residual}, \code{mean_integral_diff},
\code{mean_kl}, \code{per_probe}.
}
\description{
Benchmark selected unfolding methods on synthetic probes
}
\examples{
\donttest{
det <- Detector$new(response_function = "PTB",
                    detector_names = c("0in", "3in", "5in", "8in", "12in"))
bm <- benchmark_unfold_methods(det,
    methods = list(landweber = list(max_iterations = 20L)), n_probes = 2L)
bm$results[[1]]$mean_residual}
}
