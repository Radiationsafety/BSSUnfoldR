% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_sandii.R
\name{solve_sandii}
\alias{solve_sandii}
\title{SAND-II unfolding}
\usage{
solve_sandii(
  A,
  b,
  x0,
  max_iterations = 50L,
  tolerance = 0.001,
  chi_fac = 1L,
  relative_uncertainty = 0.1,
  sigma = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum guess (length n).}

\item{max_iterations}{Positive integer; default 50.}

\item{tolerance}{Positive numeric; default 1e-3.}

\item{chi_fac}{Integer; 1 = stop on chi-square, 0 = stop on relative change.
Default 1.}

\item{relative_uncertainty}{Numeric; default 0.1.}

\item{sigma}{Optional explicit per-detector uncertainties. Default \code{NULL}.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Port of the SAND-II multiplicative ratio algorithm. Each iteration corrects
every spectrum bin by the weighted geometric mean of the
measured-to-calculated count-rate ratios.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_sandii(A, b, rep(1, 3), max_iterations = 20)
}
