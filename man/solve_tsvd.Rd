% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_tsvd.R
\name{solve_tsvd}
\alias{solve_tsvd}
\title{TSVD (Truncated SVD) unfolding}
\usage{
solve_tsvd(
  A,
  b,
  x0 = NULL,
  method = "discrepancy",
  k = NULL,
  threshold = NULL,
  noise_level = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Ignored; present for API compatibility.}

\item{method}{Character; k-selection method: 'discrepancy', 'l_curve',
'gcv', 'energy', 'threshold_ratio', 'median_threshold', 'donoho',
'mean_threshold'. Default 'discrepancy'.}

\item{k}{Optional integer; fixed truncation parameter. Overrides
\code{method}. Default \code{NULL}.}

\item{threshold}{Optional numeric; threshold ratio for truncation.
Default \code{NULL}.}

\item{noise_level}{Optional numeric noise level for discrepancy principle.
Default \code{NULL}.}
}
\value{
A list \code{list(spectrum, iterations = k, converged = TRUE)}.
}
\description{
Solves \eqn{A x = b} via truncated SVD: \eqn{x = V_k S_k^{-1} U_k^T b}.
The truncation parameter k can be set explicitly, by threshold ratio, or
selected automatically by one of several criteria (discrepancy principle,
L-curve, GCV, etc.).
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_tsvd(A, b, NULL, method = "gcv")
}
