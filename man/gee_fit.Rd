% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_gee.R
\name{gee_fit}
\alias{gee_fit}
\title{Fit the unfolding model by generalized estimating equations}
\usage{
gee_fit(
  A,
  b,
  x0 = NULL,
  family = "gaussian",
  corstr = "exchangeable",
  regularization = 1e-04,
  max_iterations = 100L,
  tolerance = 1e-06,
  diff_order = 2L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial spectrum guess.}

\item{family}{Character: \code{"gaussian"} (default), \code{"poisson"}
or \code{"gamma"}.}

\item{corstr}{Character: \code{"exchangeable"} (default), \code{"ar1"}
or \code{"independence"}.}

\item{regularization}{Numeric; relative ridge on the difference
penalty. Default 1e-4. \code{0} disables it.}

\item{max_iterations}{Integer; max GEE iterations. Default 100.}

\item{tolerance}{Numeric; relative convergence tolerance. Default 1e-6.}

\item{diff_order}{Integer; order of the roughness penalty. Default 2.}
}
\value{
A diagnostics list with \code{spectrum}, \code{cov_robust},
\code{cov_naive}, \code{robust_se}, \code{naive_se}, \code{alpha},
\code{phi}, \code{residuals}, \code{pearson_residuals},
\code{pearson_chi2}, \code{df}, \code{iterations}, \code{converged},
\code{family}, \code{corstr}.
}
\description{
Fit the unfolding model by generalized estimating equations
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
d <- gee_fit(A, b, family = "gaussian", corstr = "exchangeable",
             max_iterations = 50)
}
