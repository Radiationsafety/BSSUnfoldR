% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_doroshenko.R
\name{solve_doroshenko}
\alias{solve_doroshenko}
\title{Doroshenko coordinate-update unfolding}
\usage{
solve_doroshenko(
  A,
  b,
  x0,
  max_iterations = 1000L,
  tolerance = 1e-06,
  regularization = 0
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial guess (length n).}

\item{max_iterations}{Positive integer; default 1000.}

\item{tolerance}{Positive numeric; default 1e-6.}

\item{regularization}{Non-negative numeric; regularization strength to
prevent division by zero. Default 0.0.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_doroshenko.py}. Uses incremental
residual update for O(n) per-coordinate complexity instead of O(n^2) from
full matrix-vector products.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_doroshenko(A, b, rep(1, 3), max_iterations = 30)
}
