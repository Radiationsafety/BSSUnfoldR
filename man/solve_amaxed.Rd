% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_amaxed.R
\name{solve_amaxed}
\alias{solve_amaxed}
\title{AMAXED (Alternative MAXED) unfolding}
\usage{
solve_amaxed(
  A,
  b,
  x0,
  sigma_factor = 0.1,
  target_chi2 = NULL,
  max_iterations = 5000L,
  tolerance = 1e-08,
  line_search_tol = 1e-06
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric reference (prior) spectrum (length n).}

\item{sigma_factor}{Numeric; relative measurement uncertainty. Default 0.1.}

\item{target_chi2}{Optional numeric; target chi-squared. Default \code{NULL}
= \code{m} (number of measurements).}

\item{max_iterations}{Positive integer; default 5000.}

\item{tolerance}{Positive numeric; gradient convergence tolerance. Default 1e-8.}

\item{line_search_tol}{Numeric; line search tolerance. Default 1e-6.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_amaxed.py}.
Implements Alternative Maximum Entropy Deconvolution (Wong 2024) with
reversed cross-entropy definition compared to MAXED, using Newton's
method with backtracking line search on the KKT residual.
}
\details{
Minimises KL divergence while adhering to the chi-squared constraint
using Lagrangian multipliers. The work happens on the normalized simplex
(phi sums to 1) and the result is rescaled at the end.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_amaxed(A, b, rep(1, 3), max_iterations = 50)
}
