% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_fista.R
\name{unfold_fista}
\alias{unfold_fista}
\title{Wrapper around \code{\link{solve_fista}} for the unified workflow.}
\usage{
unfold_fista(
  detector_names,
  n_energy_bins,
  E_MeV,
  sensitivities,
  cc_icrp116,
  save_result_callback,
  readings,
  initial_spectrum = NULL,
  max_iterations = 500L,
  tolerance = 1e-08,
  regularization = 0,
  l1_penalty = 0,
  tv_penalty = 0,
  nonnegativity = TRUE,
  x_min = 0,
  x_max = Inf,
  noise_level = NULL,
  eta = 1.01,
  calculate_errors = FALSE,
  n_montecarlo = 100L,
  save_result = FALSE,
  random_state = NULL
)
}
\arguments{
\item{detector_names}{Character vector of detector names.}

\item{n_energy_bins}{Integer; number of energy bins in the output spectrum.}

\item{E_MeV}{Numeric energy grid (length \code{n_energy_bins}).}

\item{sensitivities}{Named list of numeric sensitivity vectors, one per
detector (each of length \code{n_energy_bins}).}

\item{cc_icrp116}{Named list of ICRP-116 conversion coefficients (or
\code{NULL} to skip dose-rate computation).}

\item{save_result_callback}{Function of one argument (the result list)
invoked when \code{save_result = TRUE}. Default \code{NULL} = no saving.}

\item{readings}{Named numeric vector of detector readings.}

\item{initial_spectrum}{Optional numeric spectrum guess (length
\code{n_energy_bins}) or \code{NULL} to use \code{default_initial}.}

\item{max_iterations}{Positive integer; default 500.}

\item{tolerance}{Positive numeric; default 1e-8.}

\item{regularization}{Numeric Tikhonov regularization parameter. Default 0.}

\item{l1_penalty}{Numeric L1 penalty for sparsity. Default 0.}

\item{tv_penalty}{Numeric total-variation penalty. Default 0.}

\item{nonnegativity}{Logical; enforce non-negativity. Default \code{TRUE}.}

\item{x_min, x_max}{Numeric box bounds. Default \code{0} and \code{Inf}.}

\item{noise_level}{Numeric; relative Gaussian noise level for Monte-Carlo.
Default 0.01.}

\item{eta}{Numeric; safety factor for discrepancy principle. Default 1.01.}

\item{calculate_errors}{Logical; if \code{TRUE}, run Monte-Carlo
uncertainty estimation. Default \code{FALSE}.}

\item{n_montecarlo}{Integer; number of Monte-Carlo samples. Default 100.}

\item{save_result}{Logical; if \code{TRUE}, call
\code{save_result_callback}. Default \code{FALSE}.}

\item{random_state}{Optional integer seed for Monte-Carlo.}
}
\value{
A result list as produced by \code{\link{run_unfolding}}.
}
\description{
Wrapper around \code{\link{solve_fista}} for the unified workflow.
}
