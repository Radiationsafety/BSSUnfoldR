% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_sart.R
\name{solve_sart}
\alias{solve_sart}
\title{SART (simultaneous algebraic reconstruction technique) unfolding}
\usage{
solve_sart(
  A,
  b,
  x0,
  max_iterations = 50L,
  tolerance = 1e-06,
  relaxation = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{max_iterations}{Positive integer; default 50.}

\item{tolerance}{Positive numeric; default 1e-6.}

\item{relaxation}{Numeric constant (default 0.8) or a function
\code{function(n)} returning the relaxation at iteration n.
Default \code{NULL} = constant 0.8.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_sart.py}. A relaxed weighted
least-squares algebraic reconstruction:
\deqn{x^{n+1} = x^n + alpha(n)/(A^T 1 + eps) * A^T ((b - A x^n) / (A 1 + eps))}
The residual is normalised by the forward-projected unit image (\code{A 1})
and the update by the back-projected unit image (\code{A^T 1}).
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_sart(A, b, rep(0, 3), max_iterations = 50)
}
