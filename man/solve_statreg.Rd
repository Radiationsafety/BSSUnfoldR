% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_statreg.R
\name{solve_statreg}
\alias{solve_statreg}
\title{Statistical Regularization (Turchin's method) unfolding}
\usage{
solve_statreg(
  A,
  b,
  x0 = NULL,
  E_MeV = NULL,
  unfoldermethod = "EmpiricalBayes",
  regularization = NULL,
  basis_name = "CubicSplines",
  boundary = NULL,
  derivative_degree = 2L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Ignored; accepted for API compatibility.}

\item{E_MeV}{Optional energy grid (used only for log-energy penalty scaling
in the Python version; here kept for API compatibility).}

\item{unfoldermethod}{Character; \code{"EmpiricalBayes"} (L-curve, default)
or \code{"User"} (fixed alpha).}

\item{regularization}{Optional numeric; alpha for \code{"User"} method.}

\item{basis_name}{Character; ignored (API compatibility).}

\item{boundary}{Character; ignored (API compatibility).}

\item{derivative_degree}{Integer; derivative order for penalty (only 2
implemented). Default 2.}
}
\value{
A list \code{list(spectrum, iterations = 0L, converged = TRUE)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_statreg.py}. Implements
Turchin's method of statistical regularization:
\deqn{\hat{\phi} = \arg\min \{ 1/2 \|\Sigma^{-1/2}(A\phi - b)\|^2 + 1/2 \alpha \|D_2 \phi\|^2 \}}
where \eqn{D_2} is the second-order finite-difference operator. The
regularization parameter \eqn{\alpha} is selected automatically via the
L-curve (maximum curvature) or set by the user.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_statreg(A, b, NULL, E_MeV = c(1e-9, 1e-6, 1e-3))
}
