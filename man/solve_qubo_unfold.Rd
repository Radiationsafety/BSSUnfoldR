% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_meta_heuristic.R
\name{solve_qubo_unfold}
\alias{solve_qubo_unfold}
\title{QUBO quantum-inspired unfolding (simulated annealing on binary encoding)}
\usage{
solve_qubo_unfold(
  A,
  b,
  x0 = NULL,
  n_bits = 4L,
  max_iterations = 500L,
  tolerance = 1e-06,
  random_state = NULL
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial spectrum (warm-start). Default \code{NULL} = flat.}

\item{n_bits}{Integer; bits per energy bin. Default 4.}

\item{max_iterations}{Integer; max SA iterations. Default 500.}

\item{tolerance}{Numeric; convergence tolerance. Default 1e-6.}

\item{random_state}{Optional seed.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Encodes the spectrum as a binary vector and solves the QUBO
\eqn{\min y^T Q y + c^T y} via simulated annealing.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
set.seed(7)
r <- solve_qubo_unfold(A, b, rep(1, 3), n_bits = 3,
                        max_iterations = 100)
}
