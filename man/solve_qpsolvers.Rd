% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_optimization.R
\name{solve_qpsolvers}
\alias{solve_qpsolvers}
\title{QP solver unfolding (Tikhonov-regularized NNLS)}
\usage{
solve_qpsolvers(A, b, x0 = NULL, alpha = 0.01, smoothness_order = 0L)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Unused.}

\item{alpha}{Numeric; regularization. Default 0.01.}

\item{smoothness_order}{Integer; 0, 1, or 2. Default 0.}
}
\value{
A list \code{list(spectrum, iterations = 1, converged = TRUE)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_qpsolvers.py}.
Solves \eqn{\min \|A x - b\|^2 + \alpha \|L x\|^2} with \eqn{x \geq 0}
via the augmented-matrix NNLS approach.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_qpsolvers(A, b, NULL, alpha = 0.01)
}
