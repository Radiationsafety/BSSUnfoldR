% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_express.R
\name{solve_express}
\alias{solve_express}
\title{Express piecewise-exponential unfolding}
\usage{
solve_express(
  A,
  b,
  E,
  x0 = NULL,
  n_groups = 6L,
  interval_boundaries = NULL,
  max_iterations = 3L,
  tol_iteration = 0.05,
  relative_uncertainty = 0.05
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{E}{Numeric energy grid (length n).}

\item{x0}{Optional initial spectrum guess. Default \code{NULL} = flat.}

\item{n_groups}{Integer; number of piecewise-exponential groups. Default 6.}

\item{interval_boundaries}{Optional numeric vector of group boundary
energies. Default \code{NULL} = evenly spaced.}

\item{max_iterations}{Integer; maximum number of function evaluations
(will be multiplied by 100 internally). Default 3.}

\item{tol_iteration}{Numeric; convergence tolerance on relative residual.
Default 0.05.}

\item{relative_uncertainty}{Numeric; relative measurement uncertainty.
Default 0.05.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_express.py}. Fits a
piecewise-exponential spectrum (linear in log-space between boundaries)
directly to sphere readings via nonlinear least-squares.
}
\examples{
E <- 10^seq(-9, 1, length.out = 10)
A <- matrix(runif(3 * 10), nrow = 3)
b <- as.numeric(A \%*\% rep(0.5, 10))
r <- solve_express(A, b, E, n_groups = 4)
}
