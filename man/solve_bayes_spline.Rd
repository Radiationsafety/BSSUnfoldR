% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_bayes_spline.R
\name{solve_bayes_spline}
\alias{solve_bayes_spline}
\title{Bayesian iterative unfolding with spline regularization}
\usage{
solve_bayes_spline(
  A,
  b,
  x0 = NULL,
  max_iterations = 4000L,
  tolerance = 0.001,
  spline_degree = 3L,
  spline_smooth = 0.01
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional prior spectrum. Default \code{NULL} = uniform.}

\item{max_iterations}{Positive integer; default 4000.}

\item{tolerance}{Positive numeric; default 1e-3.}

\item{spline_degree}{Integer; spline degree (1-5). Default 3.}

\item{spline_smooth}{Numeric; smoothing parameter passed to
\code{\link[stats]{smooth.spline}}. Default 1e-2.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/src/bssunfold/core/unfold_bayes_spline_regularization.py}.
D'Agostini iterative Bayesian unfolding with a smoothing spline applied
to the physical spectrum between iterations. The smoother is applied in
log10-space to handle the large dynamic range of physical spectra and to
prevent edge blow-up in low-sensitivity bins.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_bayes_spline(A, b, rep(1, 3), max_iterations = 50)
}
