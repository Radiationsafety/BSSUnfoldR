% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_qp_engines.R
\name{solve_qp_docplex}
\alias{solve_qp_docplex}
\title{Tikhonov QP solve via the CPLEX-style engine (pure-R analogue)}
\usage{
solve_qp_docplex(
  A,
  b,
  x0 = NULL,
  regularization = 1e-04,
  norm = 2L,
  smoothness_order = 2L,
  smoothness_weight = 1e-05,
  max_iterations = 3000L,
  tolerance = 1e-08
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum (length n).}

\item{regularization}{Numeric Tikhonov weight. Default 1e-4.}

\item{norm}{Integer (1 or 2) penalty norm. Default 2.}

\item{smoothness_order}{Integer derivative order for the smoothness term.
Default 2.}

\item{smoothness_weight}{Numeric smoothness weight. Default 1e-5.}

\item{max_iterations}{Integer PG iterations. Default 3000.}

\item{tolerance}{Numeric relative tolerance. Default 1e-8.}
}
\description{
Tikhonov QP solve via the CPLEX-style engine (pure-R analogue)
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05, 0.10, 0.8, 0.10, 0.30, 0.30, 0.40),
            nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_qp_docplex(A, b, rep(1, 3), max_iterations = 500L)
}
