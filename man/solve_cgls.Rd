% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_cgls.R
\name{solve_cgls}
\alias{solve_cgls}
\title{CGLS (Conjugate Gradient Least Squares) unfolding}
\usage{
solve_cgls(
  A,
  b,
  x0 = NULL,
  max_iterations = 100L,
  tolerance = 1e-12,
  noise_level = NULL,
  regularization = 0,
  smoothness_order = 0L
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional initial guess (length n). Default \code{NULL} = zero vector.}

\item{max_iterations}{Positive integer; default 100.}

\item{tolerance}{Positive numeric; default 1e-12.}

\item{noise_level}{Optional relative noise level used for
discrepancy-principle stopping. Default \code{NULL}.}

\item{regularization}{Numeric Tikhonov parameter. Default 0.0.}

\item{smoothness_order}{Integer; 0 (identity), 1 or 2. Default 0.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Applies the conjugate-gradient algorithm implicitly to the normal equations
\eqn{A^T A x = A^T b}{A'A x = A'b}. A regularized solution is obtained by
early termination of the iterations (semi-convergence). Optionally a
Tikhonov term \eqn{lambda^2 ||L x||^2} may be added.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_cgls(A, b, rep(0, 3), max_iterations = 50)
}
