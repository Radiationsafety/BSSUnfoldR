% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_landweber.R
\name{solve_landweber}
\alias{solve_landweber}
\title{Landweber iteration unfolding}
\usage{
solve_landweber(A, b, x0, max_iterations = 1000L, tolerance = 1e-06)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum guess (length n).}

\item{max_iterations}{Positive integer; default 1000.}

\item{tolerance}{Positive numeric; convergence tolerance on the residual
norm. Default 1e-6.}
}
\value{
A list \code{list(spectrum = ..., iterations = ..., converged = ...)}.
}
\description{
Iterative Landweber method for solving \eqn{A x = b}:
\eqn{x_{k+1} = P_+(x_k - tau * A^T (A x_k - b))}{x_{k+1} = P+(x_k - tau * A'(A x_k - b))}
with step size \eqn{tau = 1 / sigma_max^2}{tau = 1 / sigma_max^2}.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_landweber(A, b, rep(0, 3), max_iterations = 50)
r$converged
}
