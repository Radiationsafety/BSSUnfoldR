% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_ferdor.R
\name{solve_ferdor}
\alias{solve_ferdor}
\title{FERDOR (Ferret) unfolding}
\usage{
solve_ferdor(
  A,
  b,
  x0,
  max_iterations = 100L,
  tolerance = 0.001,
  smoothing = 0.001,
  chi_squared_target = 1,
  relative_uncertainty = 0.1,
  sigma = NULL,
  min_alpha = 1e-12,
  max_alpha = 1e+12
)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Numeric initial spectrum guess (length n) -- accepted for API
compatibility; the solution does not depend on it.}

\item{max_iterations}{Positive integer; default 100.}

\item{tolerance}{Positive numeric; default 1e-3.}

\item{smoothing}{Numeric; initial smoothing weight alpha. Default 1e-3.}

\item{chi_squared_target}{Numeric; default 1.0.}

\item{relative_uncertainty}{Numeric; default 0.1.}

\item{sigma}{Optional per-detector uncertainties. Default \code{NULL}.}

\item{min_alpha}{Numeric; default 1e-12.}

\item{max_alpha}{Numeric; default 1e12.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
Classic weighted least-squares with second-difference smoothing. The
smoothing weight \eqn{alpha} is adjusted iteratively (bisection) so the
reduced chi-square approaches \code{chi_squared_target} (discrepancy
principle). Non-negativity is enforced via the \pkg{lsei} NNLS solver.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_ferdor(A, b, rep(1, 3))
}
