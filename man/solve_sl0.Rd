% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_cs.R
\name{solve_sl0}
\alias{solve_sl0}
\title{Smoothed L0 (SL0) reconstruction}
\usage{
solve_sl0(
  A,
  b,
  sigma_min = 0.01,
  sigma_decrease_factor = 0.5,
  mu_0 = 1,
  L = 3L,
  max_iterations = 1000L,
  tolerance = 1e-06
)
}
\arguments{
\item{A}{Numeric sensing matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{sigma_min}{Numeric; minimum sigma. Default 0.01.}

\item{sigma_decrease_factor}{Numeric; sigma decrease per outer iter. Default 0.5.}

\item{mu_0}{Numeric; step-size factor. Default 1.0.}

\item{L}{Integer; inner steepest-descent iterations per sigma. Default 3.}

\item{max_iterations}{Integer; max outer iterations. Default 1000.}

\item{tolerance}{Numeric; convergence tolerance. Default 1e-6.}
}
\value{
Numeric vector (length n).
}
\description{
Approximates the L0 norm by a smooth surrogate (Gaussian) and performs
a steepest-descent / projection iteration to find the sparsest solution
of the underdetermined linear system \eqn{b = A x}.
}
\examples{
A <- matrix(runif(3 * 10), nrow = 3)
b <- as.numeric(A \%*\% c(1, 0, 0, 0, 2, 0, 0, 0, 0, 0))
x <- solve_sl0(A, b)
}
