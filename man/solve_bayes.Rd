% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unfold_bayes.R
\name{solve_bayes}
\alias{solve_bayes}
\title{Bayesian iterative unfolding (D'Agostini)}
\usage{
solve_bayes(A, b, x0 = NULL, max_iterations = 4000L, tolerance = 0.001)
}
\arguments{
\item{A}{Numeric response matrix (m x n).}

\item{b}{Numeric measurement vector (length m).}

\item{x0}{Optional prior spectrum (length n). Default \code{NULL} = uniform.}

\item{max_iterations}{Positive integer; default 4000.}

\item{tolerance}{Positive numeric; relative L2 convergence tolerance.
Default 1e-3.}
}
\value{
A list \code{list(spectrum, iterations, converged)}.
}
\description{
R port of \code{bssunfold/core/unfold_bayes.py}. Pure-R implementation of
the D'Agostini algorithm. The response matrix is column-normalised so each
column sums to 1 (conditional probability \eqn{P(D_j | E_i)}), then the
result is rescaled to physical units via division by the column sums.
}
\examples{
A <- matrix(c(0.9, 0.05, 0.05,
              0.10, 0.8, 0.10,
              0.30, 0.30, 0.40), nrow = 3, byrow = TRUE)
b <- c(1, 0.6, 0.4)
r <- solve_bayes(A, b, rep(1, 3), max_iterations = 50)
}
